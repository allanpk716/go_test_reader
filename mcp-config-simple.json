{
  "mcpServers": {
    "go-test-reader": {
      "command": "go_test_reader.exe",
      "args": []
    }
  }
}

// 注意：此配置适用于只支持基本 MCP 配置的 IDE
// 使用前请确保：
// 1. 已编译项目：go build -o go_test_reader.exe
// 2. 将 go_test_reader.exe 添加到系统 PATH 环境变量中
// 3. 或者将 go_test_reader.exe 复制到 IDE 的工作目录

// 如果你的 IDE 支持完整路径，可以使用：
// "command": "C:\\WorkSpace\\Go2Hell\\src\\github.com\\allanpk716\\go_test_reader\\go_test_reader.exe"